import { createMuiTheme } from 'material-ui/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#d52728',
    },
    secondary: {
      main: '#404CFA',
    },
  },
});

export default theme;